# === LangChain Modern Stack (Python 3.11.4) ===
langchain==0.3.7
langchain-community==0.3.7
langsmith==0.1.125

# === Gemini Integration ===
# Pinned to 2.0.x: v4+ requires langchain-core>=1.2.5, conflicting with 0.3.x stack.
# google-generativeai intentionally NOT pinned: 0.8.3 hard-pins
# google-ai-generativelanguage==0.6.10, but langchain-google-genai 2.0.11 needs >=0.6.16.
# Removing the explicit pin lets pip pull in a compatible version automatically.
langchain-google-genai==2.0.11

# === LangGraph (Modern Compatible) ===
langgraph==0.2.34

# === Travel APIs ===
amadeus==10.0.0

# === Optimization ===
ortools==9.10.4067

# === Utilities ===
python-dotenv==1.0.1
requests==2.32.3

# === Data ===
numpy==1.26.4
pandas==2.2.2

# === Structured Output ===
pydantic==2.8.2